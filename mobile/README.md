Mobile app (Expo)

Run:

```powershell
cd mobile
npm install
npx expo start
```

Notes:
- Update `API_BASE` in `services/AnalysisService.js` to point to your backend reachable IP when testing on physical devices.
